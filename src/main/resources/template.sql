-- IMPORTANT !! DONT DELETE template.sql, its used in script
-- This is a Flyway migration script
-- It will be executed to update the database schema

-- Placeholder for version and description
-- Version: << FlywayVersion >> Description: << Description >>

-- Change the schema version
-- Place your SQL statements here to modify the database schema
-- For example, creating or altering tables, indexes, or views

-- Example: Create a new table
-- CREATE TABLE my_table (
--     id INT PRIMARY KEY,
--     name VARCHAR(255) NOT NULL
-- );

-- Example: Add a new column to an existing table
-- ALTER TABLE existing_table
-- ADD new_column INT NOT NULL;

-- Example: Create an index
-- CREATE INDEX idx_name ON my_table(name);

-- Example: Insert data
-- INSERT INTO my_table (id, name)
-- VALUES (1, 'John'), (2, 'Jane');

-- Example: Update data
-- UPDATE my_table
-- SET name = 'Updated Name'
-- WHERE id = 1;

-- Example: Delete data
-- DELETE FROM my_table
-- WHERE name = 'Jane';
